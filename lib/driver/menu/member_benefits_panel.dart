import 'package:flutter/material.dart';

class MemberBenefitsPanel extends StatelessWidget {
  const MemberBenefitsPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🎁 Driver Insurance, Bonuses & Loyalty Rewards', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
    );
  }
}