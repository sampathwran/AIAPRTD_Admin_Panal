name: aiaprtd_admin_dashboard
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.17.1 # 💡 FIXED: ආපහු ස්ථාවර වර්ෂන් එකට හැදුවා මචං
  provider: ^6.1.5+1
  firebase_core: ^4.9.0
  cloud_firestore: ^6.4.1
  cached_network_image: ^3.3.0
  async: ^2.11.0
  intl: ^0.20.2
  camera: ^0.10.6
  http: ^1.2.0
  rxdart: ^0.28.0
  firebase_auth: ^6.5.4
  firebase_storage: ^13.4.3
  crop_image: ^1.0.17
  image: ^4.9.1
  image_picker: ^1.2.3
  file_picker: ^8.1.4
  pdf: ^3.13.0
  printing: ^5.15.0
  image_cropper: ^12.2.1
  audioplayers: ^6.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/vehicle_icons/